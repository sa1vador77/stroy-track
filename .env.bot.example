# Скопируйте в .env.bot: cp .env.bot.example .env.bot
# Токен живёт отдельно от общего .env: его получает только процесс бота,
# API-контейнеру он не передаётся

# Токен телеграм-бота (выдаёт @BotFather)
BOT_TOKEN=
