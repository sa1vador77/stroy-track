# Скопируйте в .env.bot: cp .env.bot.example .env.bot
# Токен живёт отдельно от общего .env: его получают только бот и планировщик
# напоминаний, API-контейнеру он не передаётся

# Токен телеграм-бота (выдаёт @BotFather)
BOT_TOKEN=
